# Kings Defense Project Plan

## 프로젝트 개요

`kings-defense`는 Vite + Phaser 4 기반의 2D 생존/디펜스 게임입니다. 현재는 고정 세로 화면(`450 x 800`)을 뷰포트로 사용하고, 더 넓은 월드 맵에서 크게 확장된 울타리 안전지대를 출발점으로 삼아 입구 밖 적 캠프에 있는 적을 처치해 고기를 얻고, 안전지대 NPC들에게서 고기 -> 구운 고기 -> 코인으로 교환한 뒤 업그레이드하는 구조입니다.

## 현재 실행 방법

- 개발 서버: `npm run dev`
- 프로덕션 빌드: `npm run build`
- 빌드 결과 미리보기: `npm run preview`

`vite.config.js`의 `base`는 `/kings-defense/`로 설정되어 있어 GitHub Pages 같은 서브 경로 배포를 염두에 둔 상태입니다.

## 현재 기술 스택

- 번들러/개발 서버: Vite
- 게임 엔진: Phaser 4
- 언어: JavaScript ES Modules
- 주요 진입점: `src/main.js`
- 전역 스타일: `src/style.css`
- 정적 파일: `public/`

## 현재 구현된 게임 루프

- 플레이어 이동
  - WASD와 방향키 지원
  - 모바일/터치 환경에서 드래그 기반 가상 조이스틱 지원
  - 넓어진 월드 경계 안으로 위치 제한
  - 카메라가 캐릭터를 따라 움직임
  - 울타리로 감싼 안전지대는 아래쪽 입구로만 출입 가능
- 적 스폰
  - 확장된 맵의 안전지대 출구 밖 여러 적 캠프에 기존보다 2배 많은 적을 배치
  - 처치된 적은 해당 캠프 슬롯에서 일정 시간 뒤 리젠
- 적 이동/피해
  - 플레이어가 안전지대 밖에 있고 감지 범위 안에 있을 때 추적
  - 안전지대 안에서는 추적/접촉 피해가 발생하지 않음
  - 적끼리는 이동 후 최소 간격을 유지하도록 서로 밀어냄
  - 접촉 시 0.7초 쿨다운으로 HP 10 감소
  - 플레이어와 적 아래에 현재 HP 바 표시
- 자동 공격
  - 가장 가까운 적을 자동 조준해 근접 공격
  - 안전지대 안에서는 자동 공격하지 않음
  - 공격 속도, 근접 데미지, 근접 사거리 스탯 보유
- 충돌 처리
  - 근접 사거리 안의 적에게 직접 피해 적용
  - 적 사망 시 고기 생성
- 재화/업그레이드
  - 오른쪽 상단에는 현재 소지 코인 수만 배지 형태로 표시
  - 모바일 화면에서도 오른쪽 상단 HUD가 잘리지 않도록 캔버스를 뷰포트에 맞춰 스케일링
  - 안전지대 NPC는 위 중앙 UP, 좌측 COOK, 우측 SELL, 아래 중앙 동료 비콘 형태로 정렬
  - 안전지대 안 동료 비콘에서 5코인을 지불해 자동 사냥 동료를 최대 5명까지 구매 가능
  - 동료는 플레이어를 따라다니다가 근처 적을 발견하면 자동으로 접근해 근접 공격
  - 동료끼리는 이동/전투 중 최소 간격을 유지해 겹쳐 보이지 않도록 분리 처리
  - 안전지대 안 원거리 비콘에서 80코인을 지불하면 플레이어 자동 공격이 원거리 투사체 공격으로 전환
  - 고기는 근처에서 플레이어에게 끌려옴
  - 수집한 일반 고기는 캐릭터 왼쪽 뒤에 시각적으로 쌓임
  - COOK NPC 앞에 서면 캐릭터가 든 일반 고기를 0.1초마다 1개씩 자동으로 맡김
  - 맡겨진 원재료 고기는 COOK 오른쪽에 쌓임
  - COOK NPC는 0.5초마다 원재료 고기 1개를 구운 고기 1개로 변환
  - 변환된 구운 고기는 COOK NPC 왼쪽에 쌓임
  - 캐릭터가 COOK NPC 왼쪽 구운 고기 더미 앞으로 가면 0.2초마다 1개씩 수령해 캐릭터 뒤에 쌓임
  - SELL NPC 앞에 서면 캐릭터가 든 구운 고기를 0.2초마다 1개씩 자동으로 맡김
  - 맡겨진 구운 고기는 SELL NPC 옆에 쌓이고, 0.4초마다 코인으로 변환되어 반대쪽에 쌓임
  - 캐릭터가 쌓인 코인 앞으로 가면 코인을 자동 수령
  - 안전지대 안 UP NPC 근처에서 코인을 지불해 강화 선택
- 게임 오버
  - HP 0 도달 시 적/필드 고기 정리
  - 처치 수 표시 및 RESTART 버튼 제공

## 핵심 파일 구조

- `src/main.js`
  - `GameScene` 하나에 현재 게임 상태, 렌더링, 입력, 스폰, 전투, NPC 업그레이드, 게임오버 로직이 모두 들어 있습니다.
- `src/style.css`
  - 검은 배경 위에 Phaser canvas를 화면 중앙에 배치합니다.
- `index.html`
  - `#app` 컨테이너와 Vite 모듈 스크립트를 연결합니다.
- `vite.config.js`
  - 배포 base path를 `/kings-defense/`로 설정합니다.

## 현재 밸런스 값

- 화면 크기: `450 x 800`
- 월드 크기: `1200 x 1700`
- 안전지대 좌표: `left 255`, `top 70`, `right 945`, `bottom 662`
- 플레이어 HP: `1000`
- 플레이어 속도: `220`
- 적 속도: `62`, 처치 수에 따른 추가 속도 증가 없음
- 적 HP: `2`
- 적 캠프 리젠 시간: `7000ms`
- 적 감지 범위: `230`
- 적 캠프 이탈 제한 거리: `300`
- 적끼리 최소 표시 간격: `33`
- 기본 근접 공격 간격: `620ms`
- 기본 근접 공격 데미지: `1`
- 기본 근접 공격 사거리: `58`
- 원거리 비콘 비용: `80` 코인, 활성화 후 원거리 사거리: `260`
- RECOVER 회복량: `250 HP`
- 처치 고기 보상: `1`
- 구운 고기 판매가: 개당 `1` 코인
- 동료 구매 비용: `5` 코인, 최대 `5`명
- 동료 기본 속도: `185`, 탐지 범위: `360`, 공격 간격: `1040ms`, 공격 데미지: `1`, 최소 간격: `30`
- 적 접촉 피해: `10`
- 적 접촉 피해 쿨다운: `0.7s`

## 다음 개발 우선순위

1. 코드 구조 분리
   - `GameScene`에 모든 로직이 집중되어 있으므로, 다음 기능을 늘리기 전에 상수/업그레이드/엔티티 관리 로직을 분리하는 편이 좋습니다.
   - 예시: `src/config.js`, `src/upgrades.js`, `src/scenes/GameScene.js`

2. 모바일 입력 다듬기
   - 현재 터치 드래그 기반 가상 조이스틱이 구현되어 있습니다.
   - 추후 필요하면 고정 위치 조이스틱, 감도 옵션, 터치 UI 표시 조건을 개선할 수 있습니다.

3. 적 종류 확장
   - 현재 적은 HP 1짜리 단일 타입입니다.
   - 느리지만 단단한 적, 빠른 적, 원거리 적, 보스 웨이브 등을 추가하면 게임성이 크게 좋아집니다.

4. 웨이브/난이도 시스템
   - 지금은 고정된 적 캠프 슬롯이 시간에 따라 다시 채워지고, 적 속도는 고정되어 있습니다.
   - 캠프별 적 조합, 보스 캠프, 밤/낮 리젠 주기 등을 만들면 밸런싱이 쉬워집니다.

5. 업그레이드 시스템 확장
   - 현재 업그레이드는 안전지대 NPC를 통해 코인으로 구매하는 즉시 적용형입니다.
   - 희귀도, 중복 제한, 스택 수 표시, 무기별 업그레이드, 동료 강화, 상점 새로고침 비용으로 확장할 수 있습니다.

6. 피드백/연출 강화
   - 피격 플래시, 적 사망 파티클, 코인 흡수 효과, 근접 타격 이펙트, 사운드를 추가하면 완성도가 올라갑니다.

7. 저장/기록
   - 최고 처치 수, 최고 생존 시간, 구매한 업그레이드 기록을 `localStorage`에 저장할 수 있습니다.

8. 배포 점검
   - 현재 `base: '/kings-defense/'`이므로 GitHub Pages 프로젝트 페이지 배포에 맞습니다.
   - 다른 호스팅 루트에 배포할 경우 `vite.config.js`의 `base` 값을 확인해야 합니다.

## 잠재 이슈와 개선 후보

- `collectMeat()`에서 meat 이동 전 계산한 `distance`를 수집 판정에도 그대로 사용합니다. meat가 플레이어 쪽으로 이동한 뒤의 거리로 다시 판정하면 흡수감이 더 자연스러울 수 있습니다.
- `attackNearestEnemy()`는 현재 가장 가까운 적 하나만 타격합니다. 범위 공격이나 휘두르기 판정을 원하면 사거리 내 다중 대상 처리로 바꿀 수 있습니다.
- `showGameOver()`의 restart 텍스트는 버튼과 따로 생성되어 있습니다. 텍스트에도 인터랙션을 붙이면 클릭 가능 영역에 대한 체감이 좋아질 수 있습니다.
- 현재 엔티티와 울타리 충돌은 Phaser Physics를 쓰지 않고 직접 거리/경계 계산으로 처리합니다. 엔티티 수나 지형이 늘어나면 Arcade Physics 또는 타일맵 충돌 도입을 검토할 수 있습니다.
- `src/counter.js`, `src/assets/vite.svg`, `src/assets/javascript.svg`는 Vite 템플릿 잔여 파일로 보입니다. 사용하지 않는다면 정리 대상입니다.

## 추천 작업 순서

1. 사용하지 않는 Vite 템플릿 파일 정리
2. `GameScene` 파일 분리 및 상수 추출
3. 터치 조작감 미세 조정
4. 적 타입/웨이브 시스템 추가
5. 업그레이드 데이터 구조화
6. 사운드와 시각 피드백 추가
7. 최고 기록 저장
8. 빌드 후 배포 확인

## 다음에 Codex가 이어받을 때 참고할 점

- 큰 기능을 추가하기 전에는 먼저 `src/main.js`를 읽고 현재 단일 `GameScene` 구조를 확인하세요.
- 변경 후에는 최소 `npm run build`로 Vite 빌드가 통과하는지 확인하세요.
- 프론트엔드/게임 화면 변경이 크다면 개발 서버를 띄우고 브라우저에서 캔버스가 정상 렌더링되는지 확인하세요.
- 사용자가 GitHub Pages 배포를 목표로 한다면 `vite.config.js`의 `base` 설정을 유지하세요.
